name: run-bac

on:
    workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
         - name: copy the code
           uses: actions/checkout@v4

         - name: run the script
           run: ./scripts/backup.sh
           shell: bash