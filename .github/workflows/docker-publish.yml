name: Docker Publish

on: 
    push:
        branches: [main]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: checkout the code
              uses: actions/checkout@v4
            
            - name: Build Docker Image
              run: |
                docker build -t my-app devboard/.